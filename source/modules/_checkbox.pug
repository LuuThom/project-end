mixin checkbox({id, type, label, checked, disabled, valid, color, lineThrough, icon})
  .custom-control-checkbox()&attributes(attributes)
    if icon
      i(class= `${icon}`)
    input.custom-checkbox-input(
      type= type ? type : "checkbox"
      id= id ? id : null
      checked= (checked === true) ? true : false
      disabled= (disabled === true) ? true : false
      class= (color === true) ? 'custom-control' : null
      class= typeof valid !== "undefined" ? ((valid==true) ? "is--valid" : "is--invalid") : null
      class= lineThrough ? `custom-control--input-line` : null
    )
    if label
      label.custom-checkbox-label(
        for = id ? id : null
      )= label
